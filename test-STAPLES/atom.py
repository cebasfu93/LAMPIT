class atom:
    def __init__(self, name, resnum, restype):
        name=self.names
        resnum=self.resnum
        restype=self.restype

class residue:
    def __init__(self, resnum, restype):
        restype=self.restype
        resnum=self.resnum
